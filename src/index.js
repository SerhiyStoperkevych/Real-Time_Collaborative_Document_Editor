import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Example CSS file for global styles
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
